# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
#
# This must be changed for production, but we recommend not changing it in this file.
#
# See http://www.playframework.com/documentation/latest/ApplicationSecret for more details.
play.crypto.secret = "not very secret"

# The application languages
# ~~~~~
play.i18n.langs = [ "en" ]

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# play.http.router = my.application.Routes

# Database configuration
# ~~~~~
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
#
# db.default.driver=org.h2.Driver
# db.default.url="jdbc:h2:mem:play"
# db.default.username=sa
# db.default.password=""

# Evolutions
# ~~~~~
# You can disable evolutions if needed
# play.evolutions.enabled=false

# You can disable evolutions for a specific datasource if necessary
# play.evolutions.db.default.enabled=false

db {
  # You can declare as many datasources as you want.
  # By convention, the default datasource is named `default`

  # https://www.playframework.com/documentation/latest/Developing-with-the-H2-Database
  #default.driver = org.h2.Driver
  #default.url = "jdbc:h2:mem:play"
  #default.username = sa
  #default.password = ""

  # https://www.playframework.com/documentation/latest/Developing-with-the-H2-Database
  #default.driver = org.h2.Driver
  #default.driver = com.mysql.jdbc.Driver
  #default.driver = com.mysql.cj.jdbc.Driver
  #default.url = "jdbc:h2:mem:play"
  #default.url = "jdbc:mysql://labalytics.c6x7ftu8co6z.us-west-2.rds.amazonaws.com:3306/Labalytics"
  #default.url = "jdbc:mysql://labalytics.c6x7ftu8co6z.us-west-2.rds.amazonaws.com"
  #default.url = "jdbc:h2:mem:play;MODE=MYSQL"
  #default.username = labalytics
  #default.password = "labalytics"

  #default.driver=com.mysql.cj.jdbc.Driver
  default.driver=com.mysql.jdbc.Driver
  default.url="jdbc:mysql://localhost:3306/labalytics"
  #default.url="jdbc:mysql://labalytics.cxpkvk7jhq7g.us-west-2.rds.amazonaws.com:3306/labalytics"
  default.username=root
  #default.password="root"
  default.useSSL=false
<<<<<<< HEAD
  default.password="password"
=======
  default.password="root"
>>>>>>> master

  # You can expose this datasource via JNDI if needed (Useful for JPA)
  default.jndiName=DefaultDS

  # You can turn on SQL logging for any datasource
  # https://www.playframework.com/documentation/latest/Highlights25#Logging-SQL-statements
  #default.logSql=true
}

jpa.default=defaultPersistenceUnit
play.evolutions.db.default.enabled=true

server.hostname="localhost:9000"
include "mail"
